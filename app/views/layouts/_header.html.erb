<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ваша сторінка</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
      .yellow-header {
          background-color: #d9ddca; /* Жовтий колір */
      }
  </style>
</head>
<body>

<header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom yellow-header">
  <div class="col-md-3 mb-2 mb-md-0">
    <a href="/" class="d-inline-flex link-body-emphasis text-decoration-none">
      <svg class="bi" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
    </a>
  </div>
  <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
    <%= link_to "Головна сторінка", home_path,  class: "nav-link px-2 link-secondary fw-bold text-primary" %>
  </ul>
  <% if user_signed_in? %>
  <%= link_to "Профіль", user_path(current_user.id), class: "nav-link px-2 link-secondary fw-bold text-primary" %>
    <div class="col-md-3 text-end" style="margin-right: 80px;">
      <%= button_to "Вийти", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-outline-primary me-2", role: "button" %>
    </div>
  <% else %>
    <div class="col-md-3 text-end" style="margin-right: 80px;">
      <%= link_to "Увійти", new_user_session_path, class: "btn btn-outline-primary me-2", role: "button" %>
      <%= link_to "Зареєструватись", new_user_registration_path, class: "btn btn-primary", role: "button" %>
    </div>
  <% end %>
</header>

<!-- Додатковий контент вашої сторінки -->

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
